/**
 * Virtual DOM implementation.
 * 
 * A re-working of Matt Esch's Virtual-DOM relying on Typescript
 * for more safety and simplicity.
 */

import VTree = require("vtree");
 
///** h */
//export function h(tagName?: string, properties?: Props, children?: Array<VTree>): number { 
//  return 3
//}